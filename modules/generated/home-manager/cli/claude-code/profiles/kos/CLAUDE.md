<!-- READ-ONLY: Managed by NixOS. Edit the source at:
     modules/generated/home-manager/cli/claude-code/profiles/kos/CLAUDE.md

     This text is APPENDED to the shared base CLAUDE.md for the `kos` profile
     (CLAUDE_CONFIG_DIR=~/.claude-kos). Put work-profile-specific instructions
     here; the base rules above still apply. -->

## Profile: kos (work)

<!-- Add kos/work-specific rules here. Currently empty — kos behaves like the
     base profile until you add overrides. -->
